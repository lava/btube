[Unit]
Description=Simple http server providing on_publish authentication for the nginx-rtmp module

[Service]
Type=simple
ExecStart=/usr/local/bin/rtmp-authserver -c /etc/rtmp-authserver.conf
User=nobody
